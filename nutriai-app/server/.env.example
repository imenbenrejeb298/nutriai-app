# Copy to .env and set your AI provider keys here to enable AI generation
# Either set OPENAI_API_KEY (OpenAI) or ANTHROPIC_API_KEY (Anthropic Claude) or both.
# If both are present, Anthropic will be tried first.
OPENAI_API_KEY=sk-xxxx
ANTHROPIC_API_KEY=anthropic-xxxx
# Optional: override the Anthropic model name (default: claude-sonnet-3.5)
ANTHROPIC_MODEL=claude-sonnet-3.5

# JWT secret for signing user tokens (change in production)
JWT_SECRET=change_this_secret_in_production
